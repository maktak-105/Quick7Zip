# Quick7Zip フォルダ構成・文書整合 実施結果 v1.0

実施日: 2026-09-19
対応する計画書: `plans/2026-09-19_フォルダ構成と文書整合_v1.0.md`

## 1. 実施内容

- ルートのビルド補助依存ファイルを`scripts/requirements.txt`へ移動。
- READMEと日英環境構築書を現行の`scripts/`、`build/intermediate/`、`dist/`配置に更新。
- 仕様書の版数表現、概要説明、About文書を現行版v2.2.1に合わせて整理。
- 日英ペア文書冒頭の相互リンクを削除し、配布用TXTからMarkdown記法を取り除いた。
- `.gitattributes`の実態に合わないPython配置コメントを修正。
- 過去のplansは当時の作業記録として保持。

## 2. 確認結果

- 現行README、docs、scriptsに`dist/binary`、`build_native.py`、誤ったルート直下`build.bat`、`python main.py`の記述は残っていない。
- 日英開発文書はそれぞれ現行配置のビルド手順と出力先を記載。
- 配布用TXTはUTF-8 BOMなし、CRLF、Markdownバッククォートなし。
- `git diff --check`成功。
- `python scripts/build.py`成功。GUI、CLI、`WebView2Loader.dll`が`dist/`直下に生成された。
- `dist/Quick7Zip_cli.exe --help`成功（v2.2.1を表示）。テストスイートは未実施。
- GitHub ActionsのRelease Workflowはビルド後にZIPおよびバイナリのSHA-256を算出し、ZIPと`SHA256SUMS.txt`をReleaseへ添付する設定を確認。
- `git diff --check`成功。
- アイコン画像のファイル名を`assets/Quick7Zip-icon.png`へ統一。既存の古い画面キャプチャは現行UI画像と誤認されないよう、そのままREADMEには掲載していない。
